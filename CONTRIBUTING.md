# FOWOCO 기여 가이드

FOWOCO는 여러 저장소가 함께 동작하는 팀 프로젝트입니다. 큰 절차보다 **작은 변경, 분명한 이유, 재현 가능한 검증**을 우선합니다.

## 1. 작업 시작 전

1. 변경할 저장소의 `README.md`, 열린 Issue·PR, 최근 커밋 확인
2. 같은 기능을 다른 팀원이 작업 중인지 확인
3. API·DB·AI 출력 계약이 필요한 경우 관련 담당자와 먼저 합의
4. 실제 개인정보 대신 익명화·합성 데이터 사용

Issue와 브랜치, PR을 항상 1:1:1로 만들 필요는 없습니다. 다만 여러 팀에 영향을 주거나 사용자 동작이 바뀌는 작업은 Issue 또는 합의 문서로 근거를 남깁니다.

## 2. 브랜치

- 저장소에 `develop`이 있으면 기능 브랜치는 `develop`에서 시작
- `develop`이 없으면 기본 브랜치에서 시작
- `main`은 배포 가능하거나 검증된 상태 유지

권장 이름:

```text
feat/<짧은-기능명>
fix/<짧은-오류명>
data/<데이터-작업명>
docs/<문서-작업명>
chore/<관리-작업명>
infra/<인프라-작업명>
```

이미 저장소에서 Issue 번호를 브랜치에 사용하는 경우 해당 규칙을 우선합니다.

## 3. 커밋

기능 단위로 나누고 제목은 다음 형식을 권장합니다.

```text
<type>(<scope>): <한국어 요약>
```

예시:

```text
feat(task): 업무 승인 상태 API 연결
fix(document): 만료 서류가 정상으로 보이는 문제 수정
data(intent): 복합 요청 검수 데이터 추가
docs(api): 근로자 링크 응답 계약 정리
```

자주 쓰는 `type`:

| Type | 용도 |
| --- | --- |
| `feat` | 사용자 기능 추가 |
| `fix` | 오류 수정 |
| `data` | 데이터 수집·정제·라벨 변경 |
| `model` | 모델 학습·평가 변경 |
| `docs` | 문서 변경 |
| `test` | 테스트 변경 |
| `refactor` | 동작을 유지한 구조 개선 |
| `chore` | 의존성·설정·반복 관리 |
| `infra` | 배포·운영 환경 변경 |

## 4. Pull Request

- 한 PR에는 한 가지 목적을 담습니다.
- 변경 이유와 사용자가 달라지는 지점을 먼저 설명합니다.
- 관련 Issue가 없으면 `연결 작업`에 `없음`이라고 적어도 됩니다.
- 화면 변경은 개인정보를 제거한 스크린샷을 첨부합니다.
- API·DB·AI 계약이 바뀌면 영향을 받는 저장소를 명시합니다.
- 저장소에서 요구하는 테스트와 CI를 통과시킵니다.

## 5. 리뷰

리뷰어는 다음 순서로 확인합니다.

1. 사용자 흐름과 요구사항에 맞는가
2. 다른 저장소와 계약이 깨지지 않는가
3. 개인정보·권한·로그 처리가 안전한가
4. 테스트와 실패·복구 상태가 있는가
5. 코드가 기존 구조와 일관되는가

작성자는 리뷰 의견을 반영하거나, 반영하지 않는 이유를 남깁니다.

## 6. 개인정보와 AI 안전

- 실명, 외국인등록번호, 여권번호, 전화번호, 원본 신분서류를 커밋하지 않습니다.
- API Key, Token, 비밀번호, `.env` 파일을 커밋하지 않습니다.
- 로그와 테스트 결과에도 개인 식별정보를 남기지 않습니다.
- AI 결과를 법적 결론, 자동 승인, 자동 기관 제출로 표현하지 않습니다.
- 날짜·금액·문서명 등 핵심값을 생성·번역 전후에 검증합니다.

보안 문제는 공개 Issue 대신 [`SECURITY.md`](SECURITY.md)의 절차를 따릅니다.
